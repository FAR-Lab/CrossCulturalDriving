using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//Note this class should be empty and is just used as an idetefier to trigger Navigation instructions.

public class ParticipantNavigationTriggerIdent : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
